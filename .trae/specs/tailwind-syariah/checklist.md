- [x] `app.css` hanya berisi `@import "tailwindcss"`, `@theme` dengan palet syariah, dan CSS minimal
- [x] `admin.css` hanya berisi `@import "tailwindcss"`, `@theme` dengan palet syariah, dan CSS minimal
- [x] `frontend-fixes.css` tidak ada (file dihapus)
- [x] `public/build/assets/css/` — file build lama dihapus
- [x] `vite.config.js` — entry `frontend-fixes.css` dihapus
- [x] `frontend-layout.blade.php` — `<style>` block dipindah ke CSS, tidak ada inline styles
- [x] Admin dashboard (`dashboard.blade.php`) — semua `style="..."` diganti Tailwind classes
- [x] Warna primary di frontend = emerald green (`#059669`)
- [x] Warna primary di admin = emerald green (`#059669`)
- [x] Build berhasil (`npm run build` tanpa error)
- [x] Tampilan frontend konsisten (navbar, hero, cards, buttons, footer)
- [x] Tampilan admin konsisten (sidebar, header, cards, tables, buttons, forms)
